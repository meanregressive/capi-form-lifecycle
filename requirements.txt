# Python dependencies for the six scripts in scripts/.
# Install with:  pip install -r requirements.txt
# Python 3.10 or newer. Tested with openpyxl 3.1.5, lxml 5.3.0, python-docx 1.1.2, PyMuPDF 1.27 on Python 3.11.

openpyxl>=3.1      # extract_for_diff.py, validate_xlsform.py, xlsform_io.py (read/write XLSForm .xlsx; >=3.1 keeps rich text)
lxml>=5.0          # extract_for_diff.py                      (parse Word document.xml / comments)
python-docx>=1.1   # build_diff_docx.py, paper_from_xlsform.py, pdf_to_docx.py (write Word documents)
pymupdf>=1.24      # pdf_to_docx.py                           (PDF text + table extraction; the fallback when a paper exists only as PDF)
